/**
 * ONE ANSWER for unknown, legacy and wrong (ROUND_6 Task 3): three answers
 * made the form a way to ask which addresses have an account here. The form
 * offers the create door after it, which is true whether or not the address
 * exists — so the offer says nothing either.
 */
export const SIGN_IN_FAILED = 'That email and password do not match.';
